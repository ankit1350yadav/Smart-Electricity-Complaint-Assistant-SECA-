'use client';import { useState } from 'react';import { Button } from '@/components/ui/button';
export function VoiceRecorder({onText}:{onText:(text:string)=>void}){const [active,setActive]=useState(false);function start(){const SpeechRecognition=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!SpeechRecognition)return onText('Voice recognition is not supported in this browser.');const rec=new SpeechRecognition();rec.lang='en-IN';rec.onstart=()=>setActive(true);rec.onend=()=>setActive(false);rec.onresult=(e:any)=>onText(e.results[0][0].transcript);rec.start()}return <Button type="button" onClick={start}>{active?'Listening…':'Speak complaint'}</Button>}
