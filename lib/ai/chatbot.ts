import OpenAI from 'openai';
const system=`You are SECA Copilot, an electricity complaint FAQ assistant for India. Answer concise safety-first questions about outages, voltage, meters, billing, street lights, and hazards. If the user describes a complaint, ask for location and offer to file it. Never advise touching live wires, meters, transformers, or poles.`;
export async function answerFaq(message:string){if(!process.env.OPENAI_API_KEY)return{answer:`For safety, stay away from electrical equipment. Based on your question: "${message}", you can file a SECA complaint with location, photos, and contact details. For fire, shock, sparking, or hanging wire, call the local emergency electricity helpline immediately.`,suggestedAction:message.match(/complaint|outage|wire|meter|voltage|bill/i)?'file_complaint':'faq'};const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const response=await client.responses.create({model:process.env.OPENAI_MODEL||'gpt-5',input:[{role:'system',content:system},{role:'user',content:message}]});return{answer:response.output_text,suggestedAction:response.output_text.match(/file|complaint/i)?'file_complaint':'faq'}}
