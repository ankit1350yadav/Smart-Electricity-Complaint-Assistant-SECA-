import { NextResponse } from 'next/server';export async function POST(req:Request){const form=await req.formData();const file=form.get('file') as File|null;return NextResponse.json({url:file?`/uploads/${crypto.randomUUID()}-${file.name}`:null});}
